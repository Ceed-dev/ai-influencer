{
  "name": "video-analytics-hub",
  "version": "1.0.0",
  "description": "Video Analytics Hub - Cross-platform video performance analytics",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFilesAfterEnv": ["<rootDir>/gas/tests/setup.js"],
    "testMatch": ["**/gas/tests/**/*.test.js"],
    "verbose": true
  }
}
