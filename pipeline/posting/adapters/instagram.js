'use strict';

// TODO: Phase 2 - Instagram Reels posting via Instagram Graph API
// Requires: Facebook App, Instagram Business Account, access token
// Endpoint: POST /{ig-user-id}/media + POST /{ig-user-id}/media_publish

async function upload(/* { videoPath, title, description, tags } */) {
  throw new Error('Instagram posting not yet implemented (Phase 2)');
}

module.exports = { upload };
