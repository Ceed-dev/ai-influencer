'use strict';

// TODO: Phase 3 - Twitter/X video posting via X API v2
// Requires: Twitter Developer App, OAuth 2.0 bearer token
// Flow: upload media → create tweet with media_id

async function upload(/* { videoPath, title, description, tags } */) {
  throw new Error('Twitter posting not yet implemented (Phase 3)');
}

module.exports = { upload };
